# Functions for calculating stuff with some special sequences
################################################################

func tri_num(n) {
    return ($n * $n + $n) / 2;
}

################################################################